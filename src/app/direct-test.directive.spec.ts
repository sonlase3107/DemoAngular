import { DirectTestDirective } from './direct-test.directive';

describe('DirectTestDirective', () => {
  it('should create an instance', () => {
    const directive = new DirectTestDirective();
    expect(directive).toBeTruthy();
  });
});
